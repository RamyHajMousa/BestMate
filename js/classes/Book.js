export class Book {
     constructor(id, title, author, genre, imageUrl) {
          this.id = id;
          this.title = title;
          this.author = author;
          this.genre = genre;
          this.imageUrl = imageUrl;
     }
}
